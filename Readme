Create SSH key and add pub file into metatdata

$ ssh-keygen -t rsa -b 4096 -C fandodevuser



How to Login SSH

$ ssh -i dev fandodevuser@XX.XX.XX.XX


Logging into gcp sql Cloud 

$ gcloud sql connect learnwebcoading1 --user=admin --quiet
